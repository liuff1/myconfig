;; close welcome page
(setq inhibit-splash-screen t)
;; set tool bar nil
(tool-bar-mode -1)
;; set scroll bar nil
(scroll-bar-mode -1)
;; modify the cursor
(setq-default cursor-type 'bar)
;; highlight current line
;; (global-hl-line-mode t)
;; set line number
(menu-bar-mode -1)
